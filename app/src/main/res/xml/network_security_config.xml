<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is disabled globally, with explicit exceptions for localhost and the emulator loopback so
  a user can point Nexus at Ollama / LM Studio / llama.cpp on their own machine or LAN. Everything
  else must be HTTPS: an API key must never travel in the clear.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>     <!-- host machine from the emulator -->
        <domain includeSubdomains="false">10.0.3.2</domain>     <!-- Genymotion -->
        <domain includeSubdomains="false">192.168.0.0</domain>
        <domain includeSubdomains="false">192.168.1.0</domain>
    </domain-config>
</network-security-config>
